PROGRESSO - Projeto: Extracao EC - Tasy
========================================

DATA DE CRIACAO: 15/09/2026

CONTEXTO
--------
- Projeto localizado em: C:\Alexandre\Repositorios\DaVita\Extracao EC - Tasy
- Pasta raiz do workspace: C:\Alexandre\Repositorios\DaVita
- Repositorio git: sim
- Plataforma: Windows (win32)
- Shell: PowerShell 5.1

ARQUIVOS PRESENTES NO DIRETORIO
-------------------------------
- ListaEstabelecimentos.xlsx (16.496 bytes) - possivelmente lista de estabelecimentos utilizada como entrada.

MISSÂO/PERSONA
--------------
- Agir como profissional senior em Python com 10 anos de experiencia.
- Foco: extratcao de dados envolvendo sistema Tasy (Philips) - provavelmente EHR/ERP hospitalar.

HISTORICO DE CONVERSACAO
------------------------
1. Usuario pediu para atuar como profissional senior em Python com 10 anos de experiencia.
   Resposta: Confirmado e pronto para ajudar.

2. Usuario pediu para criar arquivo txt com toda a conversa e codigo para prosseguir baseado nele,
   nomeado como progresso.txt.
   -> Este arquivo foi criado em resposta a esse pedido.

STATUS
------
- Etapa inicial. Nenhum codigo desenvolvido ainda.
- Proximo passo: entender o escopo do trabalho de extracao de EC (possivelmente "Estado de Consumo" / "Extrato de Contas" / "Estabelecimentos Credenciados") do Tasy.

NOTAS PENDENTES / PROXIMOS PASSOS
---------------------------------
- Reunir especificacoes com o usuario: o que extrair, de onde (banco do Tasy ou PDFs/planilhas), formato de saida.
- Definir estrategia de extracao em Python.

3. Usuario apresentou metodo de trabalho em 4 etapas:
   1. Usuario faz a solicitacao.
   2. Profissional apresenta as solucoes e aguarda autorizacao.
   3. Usuario avalia e autoriza a implantacao.
   4. Profissional executa a acao.
   -> Combinado.

4. Usuario pediu para manter o arquivo progresso.txt atualizado com toda a conversa.
   -> Combinado. Este arquivo sera atualizado ao final de cada interacao relevante.

5. Solicitacao: criacao de automacao para extracao de dados do Tasy.
   Esclarecimentos obtidos:
   - Origem: Interface grafica do Tasy (UI automation).
   - Objetivo: dados por estabelecimento (usando ListaEstabelecimentos.xlsx).
   - Volume: sob demanda (pontual).
   - Acesso ao Tasy: usuario possui app local E versao web; foi orientado que a melhor
     opcao para automacao em Python (na epoca de decidir) sera definida na implantacao.

6. Decisao: automacao em Python.

7. ACAO EXECUTADA - Ambiente virtual:
   - Criado ambiente virtual em .venv (Python 3.14.2, pip 25.3).
   - Comando: python -m venv .venv
   - Ativacao: .venv\Scripts\python.exe

8. ACAO EXECUTADA - Arquivo de parametros:
   - Criado parametros.json com estrutura de configuracao:
     * tasy: url, usuario, senha, modo_acesso, via
     * entrada: arquivo_estabelecimentos (ListaEstabelecimentos.xlsx), planilha, coluna
     * saida: formato (xlsx), diretorio (output), arquivo
     * automacao: headless, timeout, retentativas, intervalo
   - Pendente: usuario preencher url/usuario/senha e a coluna correta da planilha.

9. ACAO EXECUTADA - Instalacao de dependencias:
   Pacotes instalados com sucesso no .venv:
   - selenium 4.49.0
   - pandas 3.0.5
   - openpyxl 3.1.5
   - numpy 2.5.3 (dependencia do pandas)
   - Outras dependencias menores (trio, urllib3, etc.)
   Nota: pyautogui NAO pôde ser instalado (sem wheel binario para Python 3.14).
          Como o fluxo sera 100% web (Edge), pyautogui nao e necessario.
   Versao do Microsoft Edge detectada: 153.0.4234.32

10. Usuario preencheu parametros.json com dados reais:
    - URL do Tasy (SSO Davita, login via digisystem.cloud)
    - Usuario: Alexandresilva3
    - Senha informada
    - Campo navegador adicionado: "edge"

11. ACAO EXECUTADA - Script de login criado e renomeado:
    - Criado script Tasy_1033.py (renomeado de tasy_login.py a pedido do usuario).
    - Funcoes: ler parametros.json, abrir Edge, localizar campos, preencher,
      efetuar login e capturar screenshots em output/.

12. Dialogo de certificado apos login:
    - Usuario confirmou que e uma janela no navegador, com UM unico certificado (do usuario).
    - Selenium consegue tratar, pois nao e janela nativa do SO.
13. Ferramenta de gravacao de cliques (Opcao B escolhida pelo usuario):
    - Criado gravar_cliques.py: abre o Edge, injeta listener JS de click/input,
      o usuario clica livremente e o script captura seletor CSS + XPath de cada
      elemento clicado, gravando em cliques_registrados.txt.
    - Objetivo: simular usuario humano (pausas naturais entre acoes).
    - Ajuste: em vez de input() (incompativel com terminal de automacao), passa a
      capturar automaticamente a cada 2s e encerra quando a janela do Edge for fechada.

14. ACOES IMPLANTADAS - Handler de certificado / popups nativos:
    - Criado certificado_handler.py (Win32 via ctypes):
      * Enumera janelas do processo msedge.exe (via EnumProcesses + psapi).
      * Detecta dialog de certificado por titulo/classe e opcionalmente por
        ausencia de titulo em dialog nativo.
      * Clica no botao OK (BM_CLICK) ou envia ENTER, e seleciona item na lista.
      * Roda em thread paralela (start/monitor/stop) enquanto o login ocorre.
      * Log completo em dialogos_nativos.log.
    - Tasy_1033.py atualizado:
      * importa NativeDialogHandler e inicia a vigia (120s) antes do submit.
      * Usa send_keys(Keys.ENTER) em vez de submit() (evita timeout do renderer).
      * page_load_strategy = "none" (nao bloqueia durante navegacao).
      * Loop de 30x (2s) alternando para novas abas e clicando popups HTML.
      * Screenshots 01..04 de evidencia.
      * Log no console com progresso [1/4]..[4/4].

15. CORRIGIDO (2026-09-15):
    - Indentacao de Tasy_1033.py corrigida (duplo dialog_handler + espacamento).

STATUS FINAL DA SESSAO 15/09
-----------------------------
- Fluxo manual confirmado: SSO -> popup download -> certificado -> dentro do sistema.
- Handler Win32 em teste, aguardando proxima rodada de validacao completa.

SESSAO 16/09
------------
16. LOGIN FUNCIONANDO:
    - Script Tasy_1033.py loga com sucesso no Tasy (SSO DaVita -> digisystem.cloud).
    - page_load_strategy = "none" (evita timeout na navegacao SSO).
    - Submit via JS: driver.execute_script("arguments[0].form.submit();", password_field)
    - Handler Win32 (certificado_handler.py) detecta e clica no dialog de certificado.
    - click_ok_in_popup trata popup HTML (checkbox do-not-ask-again + botao Fechar/OK).
    - handle_user_selection adicionado para tratar tela intermediaria de selecao de usuario.
    - Apos login, URL muda de sso.davita.com para davita.digisystem.cloud.

17. GRAVACAO DE CLIQUES:
    - gravar_cliques.py registra clicks/inputs com CSS/XPath em cliques_registrados.txt.
    - Sessoes gravadas: login + checkbox + botao Fechar.
    - Fluxo pos-login (navegacao ate relatorio 1033) ainda NAO foi gravado.

18. PYWINAUTO / AMBIENTE VIRTUAL (2026-09-16):
    - pywinauto 0.6.9 JA ESTAVA instalado no .venv.
    - Erro "No module named 'pywinauto'" no log era causado pelo script executado
      com o Python global (WindowsApps\python.exe) em vez do Python do .venv.
    - Adicionado auto-detect de venv no topo do Tasy_1033.py:
      * Se sys.prefix == sys.base_prefix (fora do venv) e .venv\Scripts\python.exe
        existe, o script relanca a si mesmo com o Python do venv.
      * Fluxo: subprocess.run([venv_python] + sys.argv) + raise SystemExit.
    - Validado: Python global detecta "fora do venv"; venv Python detecta "dentro".
    - Teste executado com sucesso (script abriu o Edge, login funcionou).

19. COMMIT (2026-09-16):
    - Commit 93d33ce: "Tasy_1033: auto-detect venv para garantir pywinauto disponivel"
    - Enviado para origin/master no GitHub.

DECISAO IMPORTANTE: LOGIN JA FUNCIONA - NAO MEXER NO FLUXO DE LOGIN
----------------------------------------------------------------------
- Todo cuidado ao alterar Tasy_1033.py ou certificado_handler.py.
- O login ja esta validado e funcionando.
- Alteracoes futuras devem ser pontuais e testadas antes de commitar.
- Agora o script pode ser executado com `python Tasy_1033.py` de qualquer lugar
  (auto-detect de venv garante o uso do .venv\Scripts\python.exe).
- Proximo passo: gravar/navegar ate relatorio 1033 e implementar extracao.

PROXIMA SESSAO (PENDENCIAS ACORDADAS)
--------------------------------------
1. AUTOMATIZAR FECHAMENTO DOS 2 POPUPS QUE APARECEM APOS O LOGIN:
   - Usuario confirmou que aparecem DOIS popups assim que loga no sistema.
   - Usar gravar_cliques.py para identificar o CSS/XPath de cada um,
     ou inspecionar via azeita (desenvolvedores).
   - Provavelmente sao gwt-Button / ngdialog (como os ja tratados).
2. NAVEGAR ATE O RELATORIO 1033:
   - Gravar com gravar_cliques.py o caminho: menu -> submenu -> relatorio 1033.
   - Registrar os cliques em cliques_registrados.txt.
3. APOS NAVEGACAO MAPEADA:
   - Implementar extracao por estabelecimento (ListaEstabelecimentos.xlsx, 148 itens).
   - Consolidar resultados em CSV gzipado.

SESSAO 17/09 - OTIMIZACAO AUTORIZADA
------------------------------------
20. Usuario reportou: login demorando muito + console continua tentando achar
    certificado mesmo apos fechar o navegador. Pediu analise SEM alteracao previa,
    depois autorizou implantar melhorias propostas.
21. ACAO EXECUTADA - Tasy_1033.py:
    - find_element: per_locator=2s com deadline total (antes: 30s por locator).
    - click_ok_in_popup / handle_user_selection / close_recorded_dialog:
      1 wait unico com XPath UNION em vez de 4-7 waits sequenciais. Timeout 1-2s.
    - Loop pos-login: sleep 1.5s, timeouts internos 1s, is_driver_alive() a cada
      iteracao. Se navegador fechado -> break + return sem screenshots.
    - dialog_handler.stop() imediato apos logado/break (antes: so no finally).
      Vigia reduzida de 120s para 60s. driver.quit() com try/except.
    - Adicionado helper is_driver_alive() via window_handles.
22. ACAO EXECUTADA - certificado_handler.py:
    - scan_interval 0.4s -> 1.0s; pid_refresh_seg=5s (antes: EnumProcesses 1024
      a cada ciclo); uia_cooldown_seg=10s apos OK.
    - pywinauto Desktop importado 1x e reutilizado (_get_desktop).
    - _log_once() evita spam de "No module named pywinauto" (cooldown 30s).
    - monitor() pula varredura Win32 durante cooldown pos-sucesso.
    - Validado: py_compile OK nos 3 scripts.
23. PENDENTE PROXIMA ETAPA (mapear navegacao 1033):
    - Usuario escolheu mapear navegacao. Aguardando gravacao com
      gravar_cliques.py do caminho menu -> relatorio 1033 com filtros
      (estabelecimento, Inicio, Final) e formato de exportacao.

24. SESSAO 17/09 - FECHADOR GENERICO DE POPUPS (AUTORIZADO PASSO A PASSO):
    - Usuario: ao logar abrem 2 popups; quer fecha-los e depois navegar ao 1033.
    - Escolhas: fechador generico + navegacao via busca superior.
    - Autorizacao parcial: implantar APENAS o fechar popups generico agora.
    - ACAO EXECUTADA - Tasy_1033.py:
      * Nova funcao fechar_popups_pos_login(timeout_total=15): loop com
        find_elements imediatos (sem waits longos), fecha em sequencia:
        botoes Fechar/OK/Okay/Entendi/gwt-Button/btn-gray dentro de
        ngdialog/dialog + icones .ngdialog-close/[aria-label=Fechar]/X.
        Marca do-not-ask-again se visivel. Clique normal + fallback JS.
      * Integrada no main apos login: screenshots 03a_antes_popup e
        03b_apos_popup + log de qtd fechada. Login anterior preservado.
      * Validado: py_compile OK.
    - PROXIMO (pendente de autorizacao): navegar_1033_via_busca().

25. SESSAO 17/09 - POPUPS REAIS IDENTIFICADOS (PRINT DO USUARIO):
    - Teste: fechador generico fechou 0. Print mostra 2 popups distintos:
      a) Bubble nativo do Edge (topo-esq): "davita.digisystem.cloud quer
         Acessar outros aplicativos e servicos" [Bloquear/Permitir] — fora do DOM.
      b) Dialog HTML central "TasyNative" com [Nao perguntar novamente] +
         [Abrir o processo de download] [Fechar] + X — sem classe gwt/ngdialog.
    - Causa raiz: (a) Selenium nao alcanca UI do navegador; (b) trava antiga
      exigia ancestor ngdialog/dialog ou classe gwt-Button, e checkbox so por
      ID do-not-ask-again — TasyNative nao tem nenhum dos dois.
    - ACAO EXECUTADA (autorizada):
      * make_driver: --deny-permission-prompts + --disable-features=
        PermissionChip,PermissionPredictionService + prefs notifications=2,
        automatic_downloads=1, protocol_handler=2 (suprime bubble a/b).
      * fechar_popups_pos_login: texto manda (Fechar/OK/Okay/Entendi sempre
        fecha, sem exigir ancestor); checkbox por texto "nao perguntar";
        nunca clica em "Abrir o processo de download".
      * Nova diagnosticar_popups(): grava botoes/inputs visiveis em
        output/diagnostico_popups.txt (ANTES/DEPOIS) + screenshots 03a/03b.
      * Validado: py_compile OK.

26. SESSAO 17/09 - FLUXO 1033 COMPLETO (AUTORIZADO):
    Roteiro do usuario (13 passos) implementado em Tasy_1033.py:
    1 Utilitarios (aba) > 2 Impressao de relatorios > 3 textbox codigo=1033
    > 4 check Relatorios do Usuario > 5 Filtrar > 6 duplo clique
    'DaVita - Etapa Conta - Por Periodo da Conta (CATE-1033)' (ActionChains).
    7 datas Inicio/Final por estabelecimento (dd/mm/yyyy da ListaEstabeleci-
    mentos.xlsx, fallback aba Parametros) > 8 titulos=ambos (radio).
    9 pasta ~/Downloads/1033 criada (pasta_downloads_1033).
    9-10 Filtros avancados > buscar estabelecimento > Selecionar.
    11 Visualizar > janela CSV > Continuar > espera download (.crdownload
    some + .csv estabiliza 2s, timeout 90s).
    12 move ~/Downloads/*.csv novo -> ~/Downloads/1033/<Estabelecimento>.csv
    (sanitizado, sufixo _1 se existir).
    13 loop do passo 10 em diante p/ todos; intervalo de automacao; contador
    ok/falhas; screenshots 05a-09_* e erros erro_*.
    - Modo teste: extracao.limite_teste=1 em parametros.json (rode 1 estab
      primeiro; zere para os 148). Login/popups preservados.
    - Validado: JSON OK, pandas OK, py_compile OK. SEM commit (aguardar teste).

27. SESSAO 17/09 - ERRO pos-duplo-clique (ValueError sheet 'lista'):
    Log: navegacao 1-6 OK (Utilitarios > Impressao > 1033 > Filtrar > duplo
    clique CATE-1033), depois crash em carregar_estabelecimentos: planilha
    'lista' (minuscula no parametros.json) vs 'Lista' (real) — pandas e
    case-sensitive. + falso positivo do fechador: clicou 'copiar url'
    (classe btn-gray) como se fosse popup.
    - CORRECAO (autorizada): aba resolvida via ExcelFile + match
      case-insensitive; xpath do fechador restrito a textos exatos
      Fechar/OK/Okay/Entendi (removidos unions por classe gwt/btn-gray)
      + checagem txt exata no loop.
    - Validado: py_compile OK; leitura real resolvida p/ 'Lista' (148,3).

28. SESSAO 17/09 - TESTE 2 NAO FUNCIONOU (SEM LOG, PC SERA REINICIADO):
    - Usuario reportou falha apos as 2 correcoes, sem enviar log desta vez.
    - Estado salvo e commitado sem diagnostico novo; pendente usuario enviar
      apos reiniciar: trecho do console + screenshot erro_* + diagnostico.
    - Hipoteses p/ proxima sessao: seletor de datas/titulos, Filtros avancados,
      Visualizar/CSV/Continuar ou espera de download.

29. SESSAO 17-18/09 - DEBUG PROFUNDO + REPLAY GRAVADO (resumo executivo):
    - Bug abertura relatorio: xpath contains(.,'CATE-1033') casava ancestrais
      (logava sucesso sem sair da Lista). Fix: nos-folha contains(text()),
      3 tentativas (double-click / ENTER / Visualizar) + verificacao real de
      navegacao via marcadores do Parametros (sem 'periodo da conta', que dava
      falso-positivo pelo nome da linha na Lista).
    - Radio Ambos clicava vizinho (preceding::input). Fix: associa input ao
      texto + verifica is_selected; depois replay usa #label_wid_10/#wid_10.
    - 'Filtro avancado' e singular (link inferior direito).
    - Popups: early-exit apos 2 passadas vazias (teto 15s->8s); clicar_por_texto
      com tag='*' otimizado p/ tags clicaveis (a/button/div/span/li).
    - Periodo virou GLOBAL em parametros.json > extracao.periodo
      (data_inicio/data_fim); preenchido 1x (era o pisca na tela). Excel agora
      so tem nomes.
    - Modal Filtro avancado mapeado via F12/DOM do usuario: tipo #WAFD-1,
      lista #table-items (scroll-item-without-headers, infinite-scroll),
      linhas tr[ng-repeat] > div.w-item-checkbox > div.check-element +
      div.w-item-label-align[data-code] > span.w-item-label-elipses.
    - Descoberta automatica: raspagem #table-items com scroll (172 itens,
      output/estabelecimentos_descobertos.xlsx). Depois virou reuso do xlsx
      (carregar_lista_descoberta) p/ eliminar scroll visivel a cada run.
    - Gravacao manual do usuario (gravar_cliques.py, 16:08-16:10, 103 linhas
      novas em cliques_registrados.txt): sequencia exata que funciona —
      duplo clique #title_1_1, Ambos #label_wid_10, #button-open-filter,
      #WAFD-1, check no div.check-element da tr[1], #submit-button,
      Visualizar, dropdown formato #detail_3_container, opcao a.opt-item-CSV,
      Continuar. Replay implementado sobre esses seletores.
    - Troca CSV -> XLSX (pedido usuario; CSV com problemas): formato XLSX
      primeiro (fallback XLS), espera download *.xlsx (timeout 120s),
      destino .xlsx. Teste limite_teste=5.
    - TESTE 5 (18/09): FALHOU — timeout aguardando XLSX no 1o item + sessao
      Edge caiu no 2o (invalid session id). Pasta 1033 vazia. Log em
      output/run_teste5.log. Download nunca validado end-to-end.
    - PENDENTE (pedido usuario p/ hoje): commit + mudancas — XLSX e padrao
      (remover selecao de formato), loop desmarca anterior e marca o proximo.

 30. SESSAO 18/09 (tarde) - REPLAY GRAVADO + LOOP COMPLETO (resumo executivo):
    - Gravacao do usuario (08:39-08:42, 18/09): fluxo completo com 3 itens.
      Diferencas vs roteiro antigo: relatorio abre via botao Imprimir
      #handlebar-1093435 (nao duplo clique); download via Exportar XLS
      #handlebar-1049356 + Continuar (XLS padrao, sem dropdown); limpeza via X
      div.w-token-clear em #checkout-content. Replay implantado fiel.
    - Pasta 1033: cria se nao existe, LIMPA no inicio da rodada cheia
      (limpar=True); modo complementar (pular_existentes=true) mantem e
      processa so os ausentes. Arquivos sobrescritos por estabelecimento.
    - Download real e TSV com extensao .xls (cabecalho 'Location Estab Conta'
      + tabs) — causa do alerta do Excel. Pipeline: valida TSV -> converte
      p/ XLSX real via pandas (alerta some). 'vazio' (so cabecalho) aceito
      sem retry; 'invalido' apaga e repete (max 3x). Contadores ok/vazios/falhas.
    - Teste 5 unidades OK (Amazonia 7635 lin, Agreste vazio). Rodada 01/05-
      31/07/2026: 165/172 (7 faltas: Cabo Frio, Gama, Ibirapuera, Recife,
      Santo Andre, Vila da Penha, DKC Osasco — 1 timeout + 6 #WAFD-1 travado).
    - Lista do modal e VIRTUALIZADA (~50 linhas no DOM, infinite-scroll;
      provado via desc_modal.html: tem Advance, nao tem Recife). Selecao com
      scroll-and-scan + early-stop; match com dobra de acento; busca digitada
      first (anti-stale: re-localiza tudo apos digitar) + fallback scroll.
    - Dimensao: painel esq tem busca #search-dimension; helper
      ativar_dimensao_estabelecimento() filtra 'Estabelecimento', clica
      #WAFD-1 fresco e verifica ativa (selected/active), com reabertura 1x.
      Fixa o bug do padrao Convenio.
    - Edge anti-bloqueio: safebrowsing/download-protection desligados
      (msg 'nao foi possivel verificar virus' barrava o .xls falso).
    - Otimizacoes velocidade: waits curtos, PNG so de erro (fotos_sucesso),
      validacao single-pass, intervalo 0, coleta JS 1 roundtrip, throttle
      is_driver_alive, limpa .crdownload no retry. Alvo ~15s/item.
    - Decisao usuario: loop usa LISTA FIXA (ListaEstabelecimentos.xlsx, 148;
      usuario mantem; 24 unidades da virtual fora da fixa foram mapeadas).
      Auditoria/verificacoes removidas — 'so ler e executar'.
    - Commits: d064c76 (replay+retry+conversao) > 240c6ea (otimizacao) >
      78d15e4 (lista fresca/pular/anti-bloqueio/WAFD) > 4377d83 (scroll-scan)
      > cfd85bd (lista fixa) > 41adaaf (busca first) > 0b5bf13 (dimensao) >
      394cf11 (varredura: fix seed, JS batch, throttle, crdownload).
    - Teste final usuario (pos-almoco): 2 unidades (Asa Norte 281 lin,
      Asa Sul - Gama), periodo novo. EXTRACAO CONSIDERADA FECHADA.

 FASE 2 - CONSOLIDACAO (iniciada 18/09 pos-almoco)
 ------------------------------------------------
 - Objetivo: consolidar os XLSX da 1033 em CSV.GZ unico.
 - Schema medido (Asa Norte): 33 colunas (Location Estab Conta...Canal).
 - Pendente: definir colunas extras (origem/periodo), delimitador, encoding,
   destino e regra de arquivos vazios/duplicados.
 - Spec fechada 18/09: 2 colunas extras (ds_modalidade='CRONICO',
   atualizacao=run YYYY-MM-DD HH:MM:SS); CSV ';' UTF-8 gzip;
   nome CRONICO-yyyyMMdd-hhmm.csv.gz em output/; inclui tudo (vazios = 0 linhas).
 - Script consolidar_ec.py criado (le 1033/*.xlsx, concatena, grava EC-*.csv.gz).
 - Decisao: trabalhar com base menor de 3 estabelecimentos
   (limite_teste=3) ate fechar a consolidacao.
 - Rodada cheia armada p/ processo completo (limite_teste=0,
   pular_existentes=false). Destino/prefixo do consolidado via
   parametros.json > saida (default ~/Downloads, CRONICO).
 - Consolidacao embutida no fluxo: Tasy_1033.py roda [5/5] automatico via
   consolidar_ec.consolidar() (com fallback p/ script manual).
   Commits: 77ef358 (consolidar_ec.py) > afe66d1 (prefixo CRONICO) >
   70112e1 (destino via params + rodada cheia) > 4dc12d4 ([5/5] no fluxo).
 - FASE 2 CONSIDERADA FECHADA (18/09).

 FASE 3 - INTUS_SC (iniciada 18/09 tarde)
 ---------------------------------------
 - Intus_SC.py: adaptado de 'Extracao EC - CSV gz/extracao.py'.
   Config via parametros.json > intus_db (credenciais ex-Config.json).
   Periodo via extracao.periodo (binds :data_inicio/:data_fim yyyy-mm-dd).
   Colunas ds_modalidade='AGUDO' + atualizacao (ex-gravacao, com segundos).
   Saida AGUDO-yyyyMMdd-hhmm.csv.gz, ';', UTF-8.
   Querys/ copiada (EC/EC_RJ/ECC.sql) com datas relativas trocadas por binds.
   Default ECC.sql (SELECT *). Deps instaladas: SQLAlchemy 2.0.54, PyMySQL 1.2.3.
 - Teste vivo 18/09 (periodo 01/05-31/08/2026): 141.762 registros em ~4min,
   AGUDO-20260918-1505.csv.gz com 64 cols validado (AGUDO + timestamp).